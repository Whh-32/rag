services:
  app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000
    # Uncomment to load .env (e.g. NEXT_PUBLIC_SEARCH_API_URL)
    # env_file: .env
    restart: on-failure
